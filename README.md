# Spiking Transformer Language Model

A minimal chatbot with options to use addition only spiking self attention (faster training and fine tuning on neuromorphic hardware) or regular self attention. Persists conversation history via JSON files, supports web interface via Gradio
