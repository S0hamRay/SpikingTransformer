# Core
torch>=2.2,<3.0
numpy>=1.24

# Tokenization
sentencepiece>=0.2.0
tiktoken>=0.6.0

# Data
datasets>=2.18.0

# Config
PyYAML>=6.0

# Chatbot interface
gradio>=4.0

# FastAPI service + auto-generated MCP server
# fastapi-mcp 0.4.x is incompatible with mcp 2.x (Server() arity change).
fastapi>=0.115
uvicorn[standard]>=0.30
fastapi-mcp>=0.3.0,<0.5
mcp>=1.6,<2

# RAG (LangGraph + Ollama + Chroma + Tavily CRAG)
langgraph>=0.2
langchain-core>=0.3
langchain-community>=0.3
langchain-ollama>=0.2
langchain-text-splitters>=0.3
langchain-tavily>=0.2
chromadb>=0.5
python-dotenv>=1.0
pypdf>=4.0
psycopg[binary]>=3.1
neo4j>=5.0

# Voice-to-text
SpeechRecognition>=3.10
# Optional STT extras (install as needed for your chosen backend):
#   pip install pyaudio            # microphone capture for the CLI (needs portaudio)
#   pip install openai-whisper     # offline transcription backend (--stt-backend whisper)
#   pip install pocketsphinx       # offline transcription backend (--stt-backend sphinx)

# Testing
pytest>=8.0
